export function CopyButtonExamples() {
  return (
    <section id="copy-button">
      <h2>Copy button</h2>
      <p>An HTML Web Component (<code>&lt;copy-button&gt;</code>) that copies the content of a <code>[data-copy-source]</code> element to the clipboard. Hidden via <code>:not(:defined)</code> when JavaScript is unavailable.</p>
    </section>
  )
}
